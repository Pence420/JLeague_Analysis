import type { RecruitmentSignal } from "@/features/league-intelligence/types";
import { ConfidenceBadge } from "@/components/ui/confidence-badge";

export function RecruitmentSignals({ items }: { items: RecruitmentSignal[] }) {
  return <section className="surface-card p-5"><div className="flex items-end justify-between"><div><p className="eyebrow">Players worth a second look</p><h2 className="module-title">Recruitment Signals</h2></div><span className="text-xs text-[var(--ink-muted)]">Opportunity, not valuation</span></div><div className="mt-5 divide-y divide-[var(--line)]">{items.slice(0, 4).map((item) => <article key={item.playerId} className="grid gap-4 py-5 first:pt-0 md:grid-cols-[1.25fr_1fr_auto] md:items-center"><div><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-full bg-[var(--ink)] text-xs font-black text-white">{item.position}</span><div><h3 className="font-bold">{item.playerName}</h3><p className="text-xs text-[var(--ink-muted)]">{item.role} · {item.teamName} · Age {item.age}</p></div></div></div><ul className="space-y-1 text-sm">{item.reasons.map((reason) => <li key={reason} className="flex gap-2"><span className="text-[var(--green)]">●</span>{reason}</li>)}<li className="flex gap-2 text-[var(--ink-muted)]"><span className="text-[var(--amber)]">▲</span>{item.risk}</li></ul><div className="min-w-24 text-right"><strong className="block text-3xl tabular-nums">{item.score}</strong><span className="block text-xs text-[var(--ink-muted)]">{item.minutes.toLocaleString()} min · {item.coverage}% coverage</span><span className="mt-2 inline-block"><ConfidenceBadge confidence={item.confidence} /></span></div></article>)}</div></section>;
}
